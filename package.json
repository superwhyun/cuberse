{
  "name": "cuberse",
  "version": "2.0.0",
  "description": "마인크래프트 스타일 큐브 쌓기 웹앱",
  "main": "server.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon server.js",
    "dev-https": "NODE_ENV=development USE_HTTPS=true nodemon server.js",
    "start": "NODE_ENV=production node server.js",
    "start-https": "NODE_ENV=production USE_HTTPS=true node server.js",
    "prod": "NODE_ENV=production node server.js",
    "service": "NODE_ENV=production node server.js",
    "vercel-build": "echo 'Build for Vercel complete'",
    "deploy": "vercel --prod",
    "headless": "npx serve .",
    "generate-ssl": "./generate-ssl.sh",
    "setup-letsencrypt": "./setup-letsencrypt.sh"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "vercel": "^28.16.0",
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
